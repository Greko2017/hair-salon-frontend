import React, { Component } from 'react'
import { connect } from 'react-redux'

export class SalesLineModal extends Component {
  render() {
    return (
      <div>
        SalesLineModal
      </div>
    )
  }
}

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = {

}

export default connect(mapStateToProps, mapDispatchToProps)(SalesLineModal)
